import 'package:flutter/material.dart';

class AkunPenjual extends StatelessWidget {
  const AkunPenjual({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Akun Penjual')),
    );
  }
}