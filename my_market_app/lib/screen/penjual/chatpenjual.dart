import 'package:flutter/material.dart';

class ChatPenjual extends StatelessWidget {
  const ChatPenjual({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('ChatPenjual')));
  }
}
