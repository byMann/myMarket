import 'package:flutter/material.dart';

class KategoriPenjual extends StatelessWidget {
  const KategoriPenjual({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Kategori Penjual')),
      // Gunakan struktur seperti `MyHomePage` dengan BottomNavigationBar khusus penjual
    );
  }
}
